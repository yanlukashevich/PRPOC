#include <stdio.h>
#include <math.h>

podaw wart

float delt(float a, float b, float c){
	float answ, e;
	if ((b*b - 4*a*c) >= 0)
		return sqrt(b*b - 4*a*c);
	else 
		return -1;
}

int znacz(float del, float a, float b, float arr[]){
	float x1, x2;
	if (del < 0){
		return -1;
	}
	if (del >= 0){
		arr[0]= (-b+del)/2a;
		arr[1]= (-b-del)/2a;
		return 0;
	}
} 

int main(){
	float a, b, c; 
	float arr[2];

}
